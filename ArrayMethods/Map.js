/* const Mapping = () => {
  const names = ["mark", "jonel", "relles"];
  const mapNames = names.map(
    (n) => n.charAt(0).toLocaleUpperCase() + n.slice(1)
  );
  return mapNames;
};

console.log(Mapping());
 */

const arr = ["mark", "jonel", "relles"];
console.log(arr.slice(1, 2));
